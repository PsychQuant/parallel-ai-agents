name: t
on: push
# EXPECT: pass
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: single-quoted run with a trailing YAML comment
        run: 'echo hi | python3 scripts/neutralise.py' # note
