# #33 verify R37 完整性審查（缺陷 c）：規則層把 `((…))` 當不透明詞，看不到裡面命令替換的 `>&2`——fd 流向規則在兩種模式都漏掉（bash 5.3 實測 PR 文字從 stderr 裸印）。
# EXPECT: rule-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: s
        shell: bash
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          (( $(echo "$PR_TITLE" >&2; echo 1) ))
          echo safe 2>&1 | python3 scripts/neutralise.py
