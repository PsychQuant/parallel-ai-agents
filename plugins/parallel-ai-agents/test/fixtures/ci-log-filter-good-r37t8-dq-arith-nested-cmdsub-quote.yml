# t8 N2（_arith_end 裡巢狀 `${…}`／`$(…)` 走各自的配對）：雙引號裡的 `$(( $(printf "%d" 1) + 1 ))` 同一行收得掉、是一個不透明的詞；拿掉巢狀配對時裡面的 `"` 讓算術配對回 None、落到「雙引號裡的算術不解析」而誤擋。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: s
        shell: bash
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          echo "$(( $(printf "%d" 1) + 1 ))" "$PR_TITLE" 2>&1 | python3 scripts/neutralise.py
