# #33 verify R37 合併時協調者發現（r37d 第 5(a) 列 `cmd_pos` 修法的相鄰輸入）：`${…}` 詞整段吃掉時 `continue`、沒維護 cmd_pos；`${CMD:-printf} ${PR_TITLE} [[ # ]]` 的 `[[` 是參數、` #` 起註解，bash 實測裸印 PR 文字。
# EXPECT: rule-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: s
        shell: bash
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          ${CMD:-printf} ${PR_TITLE} [[ # ]] 2>&1 | python3 scripts/neutralise.py
