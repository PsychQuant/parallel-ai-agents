name: t
# EXPECT: rule-red
on:
  pull_request:
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: compliant
        run: echo hi | python3 ../pai-lenses/scripts/neutralise.py
      - name: fake run in env scalar
        env:
          NOTE: |
            run: echo x | python3 ../pai-lenses/scripts/neutralise.py
        run: echo "$PR_TITLE"
