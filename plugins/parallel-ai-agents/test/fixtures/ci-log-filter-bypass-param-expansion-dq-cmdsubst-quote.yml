# #33 verify R35（opsweep --since 6cf6864 的存活者）：`${…}` 雙引號裡的命令替換自己含雙引號：`"$(echo "}| …")"`——假管線在展開裡。殺 `_param_end` 雙引號分支的 `$(` 處理（拿掉 ⇒ 內層 `"` 被當收尾 ⇒ 假管線變 code）。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: rule-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: dq-cmdsub-in-param
        run: |
          echo ${PR_TITLE#"$(echo "}| python3 scripts/neutralise.py {")"}
