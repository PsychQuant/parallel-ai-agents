# #33 verify R37 合併時協調者發現（r37d 第 5(a) 列 `cmd_pos` 修法的相鄰輸入）：`!` 只在命令位置才是保留字；`echo ! [[ # ]]` 的 `!` 是參數，舊版因它在 CMD_POS_CHARS 裡把 cmd_pos 設回 True。
# EXPECT: rule-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: s
        shell: bash
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          echo ! [[ # ]] 2>&1 | python3 scripts/neutralise.py
