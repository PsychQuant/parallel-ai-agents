name: t
on: push
# EXPECT: parse-red
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: indentation indicator must be 1-9, so this header is a YAML error
        run: |0
          echo hi | python3 scripts/neutralise.py
