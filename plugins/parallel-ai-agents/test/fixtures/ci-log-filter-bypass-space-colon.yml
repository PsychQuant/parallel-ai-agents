name: t
# EXPECT: parse-red
on:
  pull_request:
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: compliant
        run: echo hi | python3 ../pai-lenses/scripts/neutralise.py
      - name: space before colon
        run : echo "$PR_TITLE"
