name: t
on: push
# EXPECT: parse-red
jobs:
  j:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu-latest,
             macos-14]
    steps:
      - name: compliant control
        run: echo hi | python3 scripts/neutralise.py
