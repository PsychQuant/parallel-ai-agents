name: t
on: push
# EXPECT: pass
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: declaration as a trailing comment on the run line itself
        run: echo "$PR_TITLE" # LOG-FILTER: none — fixture, deliberately unfiltered
