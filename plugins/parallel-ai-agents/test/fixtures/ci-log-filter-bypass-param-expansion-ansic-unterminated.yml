# #33 verify R35（opsweep --since 6cf6864 的存活者）：`${…}` 裡的 `$'…'` 沒收尾（bash 語法錯誤）⇒ fail-closed。殺 opsweep 存活者 `_param_end` 的 `while k < n and line[k] != "'"`（拿掉 `k < n` 會越界當掉）。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: parse-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: ansic-unterm-in-param
        run: |
          echo "$PR_TITLE"
          echo ${PR_TITLE#$'abc 2>&1 | python3 scripts/neutralise.py
