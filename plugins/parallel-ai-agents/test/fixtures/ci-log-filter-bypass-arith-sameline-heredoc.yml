name: t
on: push
# EXPECT: rule-red
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: arithmetic expansion then heredoc on the same line
        run: |
          echo $((1+1)) && cat <<EOF
          x | python3 scripts/neutralise.py
          EOF
          echo "$PR_TITLE"
      - name: compliant control
        run: echo hi | python3 scripts/neutralise.py
