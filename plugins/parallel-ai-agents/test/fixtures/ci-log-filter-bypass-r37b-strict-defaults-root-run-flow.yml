# #33 verify R36 第 13 列 → R37：workflow 根層級 flow 形式的 `defaults`（`--strict`）與 `env`（兩種模式）讀不到內容 ⇒ fail-closed PARSE。
# `defaults:` 底下 `run: {shell: sh}`（R36 logic 的 defaults-run-flow）。
# LINT-ARGS: --strict
# EXPECT: parse-red
name: t
on: pull_request
defaults:
  run: {shell: sh}
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: step under a flow root run
        run: |
          set -o pipefail
          printf 'ok\n' 2>&1 | python3 scripts/neutralise.py
