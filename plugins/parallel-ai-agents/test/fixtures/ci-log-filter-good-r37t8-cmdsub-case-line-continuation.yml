# R37 完整性審查（續行重掃前還原 cases）：命令替換裡的 case 以行尾反斜線續行；重掃若留著第一趟推進的 case，收尾的 `)` 被當成模式括號、命令替換永不收尾而誤擋。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: s
        shell: bash
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          x=$(case a in \
            a) echo y ;; esac) && echo "$PR_TITLE" 2>&1 | python3 scripts/neutralise.py
