# t8 N11a（續行重掃前還原 dq_ret）：雙引號裡跨行的命令替換以行尾反斜線續行；重掃若留著第一趟推進的 dq_ret，區塊結尾會誤判「雙引號裡的命令替換沒收」。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: s
        shell: bash
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          echo "$(echo a \
          b)" "$PR_TITLE" 2>&1 | python3 scripts/neutralise.py
