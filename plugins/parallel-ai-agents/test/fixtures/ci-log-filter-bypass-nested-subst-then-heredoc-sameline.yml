name: t
on: push
# EXPECT: rule-red
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: nested command substitution closes with )) then a heredoc on the same line
        run: |
          x=$(basename $(pwd)); cat <<EOF
          x | python3 scripts/neutralise.py
          EOF
          echo "$PR_TITLE"
      - name: compliant control
        run: echo hi | python3 scripts/neutralise.py
