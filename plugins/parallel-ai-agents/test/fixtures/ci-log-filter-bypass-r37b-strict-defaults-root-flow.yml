# #33 verify R36 第 13 列 → R37：workflow 根層級 flow 形式的 `defaults`（`--strict`）與 `env`（兩種模式）讀不到內容 ⇒ fail-closed PARSE。
# `defaults: {run: {shell: sh}}`（R36 master 的寫法）：run key 守恆式本來就擋下它（`{run:` 算一個看得見的 run key）——
# 留作守衛；真正 rc=0 的是 `defaults:` 底下 `run: {…}`（見 `bypass-r37b-strict-defaults-root-run-flow`）。
# LINT-ARGS: --strict
# EXPECT: parse-red
name: t
on: pull_request
defaults: {run: {shell: sh}}
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: step under a flow root defaults
        run: |
          set -o pipefail
          printf 'ok\n' 2>&1 | python3 scripts/neutralise.py
