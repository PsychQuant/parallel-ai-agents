# #33 verify R35（opsweep 存活者：主掃描器的命令替換深度 csub／cpar）：命令替換裡的 `((1))` 是算術、由算術區塊處理，不得同時計入命令替換的括號。殺 `not line.startswith("((", i)`。
# 命令替換關掉之後的 heredoc 不在命令替換裡：內文 `EOFX` 只是資料（bash -n 通過、oracle 一致）。計數錯了 ⇒ 被當成還在命令替換裡 ⇒ 「以終止字開頭」fail-closed ⇒ 誤擋。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: csub-arith-then-heredoc
        run: |
          x=$( ((1)) )
          cat <<EOF
          EOFX
          EOF
          echo hi 2>&1 | python3 scripts/neutralise.py
