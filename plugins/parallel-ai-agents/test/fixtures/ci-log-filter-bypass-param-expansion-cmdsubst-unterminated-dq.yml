# #33 verify R35（opsweep --since 6cf6864 的存活者）：`${…}` 裡的命令替換有沒收尾的雙引號（bash 語法錯誤）⇒ fail-closed。殺 `_cmdsub_end` 的 `while k < n and line[k] != '"'`。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: parse-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: cmdsub-unterm-dq
        run: |
          echo "$PR_TITLE"
          echo ${HOME:-$(echo "abc)} 2>&1 | python3 scripts/neutralise.py
