# #33 verify R35（opsweep --since 6cf6864 的存活者）：雙引號裡**沒有**命令替換的 `<<`：只是字串。殺 `if dq_sub and …<<…` 的 `dq_sub` 運算元（拿掉 ⇒ 字串裡的 `<<` 被當 heredoc ⇒ 吞掉真管線）。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: dq-plain-lshift
        run: |
          echo "a << b" 2>&1 | python3 scripts/neutralise.py
