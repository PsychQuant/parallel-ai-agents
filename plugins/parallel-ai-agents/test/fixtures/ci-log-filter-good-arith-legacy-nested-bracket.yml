# #33 verify R35（opsweep --since 6cf6864 的存活者）：`$[ a[1] + 1 ]`：舊式算術裡的方括號計深度。殺 `brk += 1`（±1→±2 ⇒ 永遠不收尾、吞掉真管線）。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: legacy-nested-brk
        run: |
          echo $[ a[1] + 1 ] 2>&1 | python3 scripts/neutralise.py
