# #33 verify R35（opsweep --since 6cf6864 的存活者）：命令替換裡的空分隔字 heredoc：終止字是空字串，內文 `hi` 不是「以終止字開頭」的收尾（每一行都以空字串開頭）。殺 prefix 規則的 `delim` 運算元。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: cmdsub-empty-delim
        run: |
          x=$(cat <<''
          hi

          )
          echo "$x" 2>&1 | python3 scripts/neutralise.py
