name: t
on:
  pull_request:
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: compliant
        run: echo hi | python3 ../pai-lenses/scripts/neutralise.py
      - name: dash inside earlier scalar
        env:
          NOTE: |
            - name: swallowed
        run: echo "$PR_TITLE"
