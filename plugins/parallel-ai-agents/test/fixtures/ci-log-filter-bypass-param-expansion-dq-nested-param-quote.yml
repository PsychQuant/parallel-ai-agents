# #33 verify R35（opsweep --since 6cf6864 的存活者）：`${…}` 雙引號裡的巢狀 `${…}` 自己含雙引號。殺 `_param_end` 雙引號分支的 `${` 處理。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: rule-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: dq-nested-param
        run: |
          echo ${PR_TITLE#"${HOME:-"}| python3 scripts/neutralise.py {"}"}
