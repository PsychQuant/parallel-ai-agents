# R37 完整性審查（`_dq_parts`：雙引號裡的 `$X` 是展開、字面未知）：`set "-$X"` 開了什麼看不出來（X=x 時就是 xtrace）；把 `$X` 當字面時 `-$X` 不像選項、`set` 規則直接放過，bash 實測 xtrace 把 PR 文字印到 stderr。
# EXPECT: rule-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: s
        shell: bash
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          X=x
          set "-$X"
          echo "$PR_TITLE" 2>&1 | python3 scripts/neutralise.py
