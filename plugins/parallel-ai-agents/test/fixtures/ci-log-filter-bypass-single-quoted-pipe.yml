name: t
on: push
# EXPECT: rule-red
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: pipe mentioned inside single quotes is not a pipe
        run: |
          echo 'tip: pipe it with | python3 scripts/neutralise.py ok'
          echo "$PR_TITLE"
      - name: compliant control
        run: echo hi | python3 scripts/neutralise.py
