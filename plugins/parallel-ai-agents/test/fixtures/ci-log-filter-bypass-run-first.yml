# fixture for test/lint-ci-log-filter.sh --selftest（R16 requirements F3：step 以 `- run:` 開頭、沒有 name）
jobs:
  j:
    steps:
      - name: filtered
        run: echo x | python3 scripts/neutralise.py
      - run: shellcheck foo.sh
