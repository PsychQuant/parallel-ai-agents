# #33 verify R35（opsweep --since 6cf6864 的存活者）：`[[x` 是命令名、不是條件式關鍵字（`[[` 後面要是空白）——它後面的真管線是 code。殺 `[[` 後必須是空白的那個運算元。
# 預期判定用 bash 實測（oracle 一致／bash -n）。
# EXPECT: pass
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: cond-like-word
        run: |
          [[x 2>&1 | python3 scripts/neutralise.py || true
