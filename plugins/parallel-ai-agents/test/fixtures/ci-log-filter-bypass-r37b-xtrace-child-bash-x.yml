# #33 verify R36 第 8 列 → R37：xtrace 的各種拼法（set 的捆綁選項、shopt -o、非字面參數、經由 env 帶進子行程）。
# run 裡另起的 `bash -xc …`：子 bash 的 trace 行進原始 stderr（守衛：前一版的字面清單剛好抓得到——新的命令名判定不得漏；也是這條判定的 mutation 靶會翻色的那張網）（相鄰輸入，R36 沒點名）
# EXPECT: rule-red
name: t
on: pull_request
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: xtrace on a pipe-filtered step
        run: |
          bash -xc 'test -n "$PR_TITLE"'
          printf 'ok\n' 2>&1 | python3 scripts/neutralise.py
